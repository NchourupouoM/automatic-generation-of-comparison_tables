[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    llm: tests or evaluations that require a live LLM provider / API key (deselected in CI with -m "not llm")
addopts = -q
